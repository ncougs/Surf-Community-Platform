import NavigationBar from '../features/Navbar';
import CreatePost from '../features/CreatePost';

const Home = () => {
	return (
		<>
			<NavigationBar />
			<CreatePost />
		</>
	);
};

export default Home;
